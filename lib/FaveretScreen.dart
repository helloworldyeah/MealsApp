import 'package:flutter/material.dart';

class FaveretScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("FaveretScreen"),
    );
  }
}
